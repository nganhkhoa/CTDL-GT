#include <tut6lab.h>


namespace week6
{
namespace lab
{
      void labTest() {
            AVL<int>* tree = new AVL<int>();
            tree->insert(0);
            tree->insert(1);
            tree->insert(2);
      }
}    // namespace lab
namespace tut
{}    // namespace tut
namespace onclass
{}    // namespace onclass
}    // namespace week6
